#!/usr/bin/env bash
# Displays only the part of each filename after the first dash (if any)

for file in *  # Hidden files are excluded automatically
do
    # If filename contains a dash, show only part after first dash
    if [[ "$file" == *-* ]]; then
        echo "${file#*-}"
    else
        echo "$file"
    fi
done
